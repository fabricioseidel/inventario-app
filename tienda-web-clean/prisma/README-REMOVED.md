Prisma was removed from this project.

Artifacts previously in this folder (migrations, seeds) are deprecated and no longer used.
Use Supabase (PostgreSQL) and the scripts under `scripts/` for schema changes.
